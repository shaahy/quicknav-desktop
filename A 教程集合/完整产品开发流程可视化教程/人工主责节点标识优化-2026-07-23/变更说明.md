# 人工主责节点标识优化

日期：2026-07-23

补充调整：节点 09 标题由“项目初始化与智能体治理”改为“speckit初始化”。

左侧列表名称整改：合并后共有 24 个节点设置独立 `navTitle`；仅左侧节点列表使用新名称，右侧详情标题、前后节点按钮及正文继续使用原 `title`。

左侧列表名称补充调整：02 改为 `Research`，13 改为 `Spec评审`，23A 改为 `subagent-driven（主路径）`；原 28 已合并到 27。

流程合并与重排：原 27、28 合并为新 27「代码审查与反馈闭环」，左侧显示 `code-review`；原 29—35 顺次重排为 28—34，页面统计、阶段范围、CI 入口和全部上下游引用同步更新。06 左侧名称改为“原型设计”。

建议执行角色收敛：按照最终确认表，将全部 35 个可选流程节点的 `roles` 数组统一收敛为唯一核心角色；不再同时展示协作、审批或辅助角色。

## 最终变更范围

- 节点 07「团队原型评审」
- 节点 08「三文档定稿与工程交接」
- 节点 15「技术方案评审门」
- 节点 13「Spec 需求评审（可选）」
- 节点 19「Spec Kit 实施交接门」
- 节点 22「选择计划执行方式」
- 节点 28「任务循环与全分支审查门」
- 节点 32「不可变制品与 Staging」
- 节点 33「发布审批与生产部署」
- 节点 34「生产验证、回滚与交付闭环」

## 已确认设计

- 左侧节点导航和右侧详情标题仅显示醒目的红色人物图标，不显示“人工主责”文字；无障碍名称和悬停提示继续保留。
- 每个节点增加“人工执行要点”，明确主持、参与、判断及确认责任。
- 调整人工节点中容易被理解为框架主责的技能名称、角色和命令式调用示例；自动化工具只保留为辅助方法。
- 10 个人工节点将“技能”改显为“执行方式与方法”，将“调用示例”改显为“人工执行步骤”；其他节点保持原有区块名称。
- 保留评审清单、会议和版本管理等人工工作方法，不把辅助工具描述为决策主体。
- SVG、CSS 和 JavaScript 全部内嵌，HTML 继续保持无外部资源的单文件形态。

## 验证目标

- 只有最终确认的 10 个节点显示红色人物图标。
- 10 个节点均显示各自的“人工执行要点”。
- 人工节点不再把框架技能或智能体描述为最终决策主体。
- 浅色、深色、桌面和窄屏样式可读。
- 节点筛选、切换和前后导航行为不受影响。

## 验证结果

- JavaScript 语法检查通过，页面脚本共 1 段。
- DOM 检查通过：导航标识 10 个，所有人工节点的详情图标和人工执行要点均存在。
- 人工节点的执行主体检查通过：框架、智能体与流水线均未被描述为最终决策者。
- 图标可见文字为 0；无障碍名称和悬停提示保留。
- 浅色图标为 `#D92D20`，深色图标为 `#FF6B60`。
- 合并重排后的全部 35 个可选流程节点逐一切换通过，页面运行错误为 0。
- 外部资源引用为 0，仍为纯独立 HTML 单文件。
- 已生成浅色桌面和深色窄屏验证截图。
- 全部 35 个节点的“建议执行角色”均只显示 1 个唯一核心角色，并与最终确认表逐项一致。

## 左侧列表名称整改验证

- 合并后 24 个 `navTitle` 与最终名称逐项一致。
- 左侧列表显示 `navTitle`；右侧详情标题和前后节点按钮继续显示原 `title`。
- 设置 `navTitle` 的节点逐项点击后，右侧详情均显示对应的完整标题。
- 桌面与窄屏列表均无横向内容溢出，页面运行错误为 0。
