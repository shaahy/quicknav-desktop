---
topic: 本地文件导航工具
goal: 形成可供 UX、Architecture 与研发拆解使用的正式 PRD
updated: 2026-07-22T11:16
---

- (event) 用户确认按既定计划启动 bmad-prd Create 工作流，采用教练路径与愿景加功能入口
- (override) 本次明确要求全程中文；最终解析配置同样为中文，中文作为沟通与文档语言
- (decision) 产品为个人长期本地使用的桌面工具，目标是降低寻找和打开分散静态 HTML 技能速查的时间成本
- (decision) 新增 HTML 必须由用户通过系统文件选择器主动选择，禁止自动扫描磁盘或目录
- (decision) 类别是用户自建一级对象；一张卡片可属于一个或多个类别，并在每个所属类别中展示
- (decision) 首页按类别展示与管理，展示页支持快速手动切换当前类别
- (decision) 产品必须能在 Windows 与 macOS 安装和运行
- (override) Electron、Tauri、JSON、打包方式等属于 Architecture；PRD 不冻结具体技术实现
- (event) 上游输入为 brainstorming/brainstorm-local-html-skill-navigator-2026-07-21/.memlog.md
- (event) 用户调用 bmad-party-mode 辅助早期 Discovery；其观点仅用于暴露问题，不替代用户产品决策
- (event) 用户关闭 bmad-party-mode，继续 bmad-prd 教练路径；圆桌观点不视为已确认需求
- (event) Brain dump 完成：用户确认除既有脑暴 memlog 与核心事实外没有其他上游输入
- (decision) 工作方式采用教练路径与愿景加功能入口，按个人长期正式使用的严谨度逐项确认
- (override) 用户将确认方式调整为选项式：助手每轮提供少量清晰选项并标注影响，用户选择或另行补充；仍保持逐项确认
- (decision) 成功定义选择 D：查找效率为首要成功标准，结果可靠为质量底线，持续替代旧查找方式为长期结果；指标阈值后续逐项确认
- (decision) 首要效率门槛选择 A：应用已启动且目标卡片有效时，从开始查找到成功打开 HTML 不超过 5 秒且最多 2 次核心操作；核心操作口径待交互规则确认
- (decision) 同一 HTML 文件只对应一张卡片；多处展示通过多类别归属实现；重复选择时提示已收录并引导查看或编辑原卡片
- (decision) 卡片默认展示名称采用 HTML 页面标题；标题缺失或无效时回退到无扩展名文件名；用户可编辑展示名称且不影响源文件
- (decision) V1 首页卡片只展示卡片名称；不展示简介、文件名或路径，以保持快速扫读；文件信息是否用于管理场景后续确认
- (change) 用户为类别删除选择自动转入未分类；该选择与卡片必须属于用户自建类别的既有规则产生边界冲突，暂不写入 PRD，待确认未分类的正式定位
- (decision) 未分类定位选择 B：仅作为类别删除后的系统恢复类别，在首页展示但不能主动选择、重命名或删除；重新加入任一用户类别后自动离开
- (override) 此前卡片必须属于用户自建类别的规则增加恢复例外：因类别删除失去全部用户类别时可暂属系统未分类
- (decision) 删除卡片只删除应用内卡片记录和类别关系；不得删除、移动、重命名或修改原始 HTML 文件
- (decision) 多类别卡片采用两个独立动作：移出当前类别只解除当前关系；删除卡片从应用全局删除并解除全部类别关系
- (decision) 点击卡片后由系统默认浏览器打开对应本地 HTML；桌面应用不内嵌渲染 HTML，且打开操作不得修改原文件
- (decision) 页面结构选择 B：首页为常驻类别导航加当前类别卡片区；一次仅展示一个当前类别，切换不进入新页面；导航位置和控件形态留给 UX
- (decision) 类别导航包含不可重命名或删除的系统视图全部卡片；汇总时每张卡片只展示一次，不因多类别归属重复
- (decision) 启动视图选择 B：应用每次启动均进入全部卡片，不恢复上次类别，也不提供自定义启动类别
- (decision) 类别导航排序选择 A：全部卡片固定顶部；用户自建类别可手动排序；未分类仅在有卡片时出现并固定底部
- (decision) 卡片排序选择 A：全部卡片及各类别独立保存手动顺序；同一卡片在不同类别位置可不同；新卡片追加到相关视图末尾
- (decision) V1 搜索选择 A：按卡片展示名称全局搜索，不受当前类别限制；同一卡片在结果中只出现一次
- (decision) 搜索匹配选择 A：名称任意位置包含即可匹配；英文不区分大小写；忽略输入首尾空格
- (decision) 搜索结果排序选择 A：完整名称匹配优先，其次开头匹配，再其次其他包含匹配；同层沿用全部卡片手动顺序
- (decision) 源文件失效处理选择 A：保留卡片、类别关系和各视图顺序并明确标记不可用；可通过文件选择器重新选择 HTML 修复，或主动删除卡片
- (decision) 文件状态检查选择 A：启动后检查全部已收录路径，打开前再次复核；仅验证登记路径，不扫描目录、不发现或导入新文件
- (decision) 失效修复重复冲突选择 A：目标 HTML 已有卡片时阻止修复，提示已收录并定位原卡片；失效卡片保持原状
- (decision) 新增确认选择 A：选择 HTML 后始终展示默认名称和类别选择，允许修改；仅在用户确认保存后创建，取消不留临时记录
- (decision) 无用户类别时选择 A：可在卡片确认流程内创建第一个类别，创建后自动选中并继续保存，无需退出重来
- (decision) 类别命名选择 B：去除首尾空格后仅完全相同才视为重复；大小写不同可并存；空名称与系统保留名称禁止，新建和重命名规则一致
- (decision) 卡片命名选择 A：去除首尾空格后完全相同的展示名称禁止重复，大小写不同可并存；新增和重命名均适用，冲突时不得保存
- (decision) 跨设备边界选择 A：Windows 与 macOS 能力和业务规则一致但数据完全独立；V1 不提供迁移、导入导出或同步，原生系统交互可遵循各平台规范
- (change) 用户为可收录文件类型选择任意本地文件；该选择与 HTML 专属定位、默认浏览器打开及 HTML 标题规则冲突，暂不修改 PRD，待确认是否正式扩大产品定位
- (override) 产品定位由 HTML 专用导航升级为通用本地文件导航，支持用户通过文件选择器主动收录任意文件类型；禁止自动扫描的边界不变
- (override) 卡片打开行为改为调用文件对应的系统默认应用；HTML 默认名称优先读取页面标题并回退文件名，其他文件默认使用无扩展名文件名；均可人工修改
- (event) PRD 正文相关术语与标题暂缓批量更新，等待用户确认新的产品工作名后统一改写
- (decision) 产品工作名称确认为本地文件导航工具
- (event) 已按通用本地文件定位统一更新 PRD 标题、对象术语、默认名称、打开行为、异常修复与跨平台表述
- (decision) 文件选择数量选择 A：每次只选择一个本地文件并确认一张卡片；V1 不支持多选批量添加或选择文件夹导入
- (decision) 卡片辅助动作选择 A：每张可用卡片始终提供在文件管理器中显示；Windows 用资源管理器定位，macOS 用 Finder 定位，主点击仍按默认应用打开
- (decision) 无默认打开应用时选择 A：调用系统选择打开方式；用户取消不改变卡片、类别、顺序或文件状态
- (decision) 有效卡片更换源文件选择 A：编辑时可重新选择文件，保留名称、类别和排序；执行唯一性检查，冲突时不保存且继续关联原文件
- (decision) 编辑卡片时选择 A：必须保留至少一个用户自建类别，否则不能保存；只属于当前类别时不得移出，且不会主动进入未分类
- (decision) 卡片全局删除防误操作选择 A：删除前确认，明确将从所有类别移除但不影响原文件；取消则所有状态不变
- (decision) 类别删除确认选择 A：任何用户类别删除前均需确认，并显示类别卡片总数及将进入未分类的数量；取消不改变状态
- (decision) 首次启动选择 A：不预置用户类别，只显示空的全部卡片，未分类隐藏；首次新增流程内创建第一个类别
- (decision) 文件路径展示选择 A：管理界面只读显示完整路径，支持复制和在文件管理器中显示；不得直接编辑路径，更换文件必须通过选择器
- (decision) 可执行文件边界选择 C：允许收录并按系统默认方式直接打开或运行，不增加产品额外警告；不得绕过 Windows 或 macOS 自身安全提示
- (decision) 数据持久性选择 A：正常关闭、系统重启和应用升级后保留全部应用数据；卸载后保留及重装恢复不属于 V1 保证
- (decision) 容量验收选择 D：V1 不声明卡片或类别上限，不做容量指标验收；不等于无限容量，实际规模留待 Architecture 和实现验证
- (decision) 存储位置选择 A：允许选择系统可访问的内置、外接、网络和云盘同步位置；暂不可访问时标记不可用，路径恢复后在下一次检查自动恢复
- (decision) 长期成功验收选择 D：取消长期使用占比指标，仅保留效率指标与可靠性要求
- (override) 成功定义由原三层调整为两层；长期形成习惯仅作为产品愿景，不作为 V1 验收指标
- (decision) 可靠性验收选择 B：成功指标只评价点击卡片后的打开结果，必须成功交给系统或明确失败和后续动作，不得静默；启动检查仍是功能要求但不计入成功指标
- (change) 用户要求 V1 新增每张卡片可设置纯文字备注；备注的展示、搜索和长度规则待逐项确认，暂不改变首页仅名称及搜索仅名称的既有规则
- (decision) 卡片备注采用自定义方案：首页展示名称和纯文字备注摘要，备注不参与搜索；无备注时只显示名称
- (override) 此前首页卡片只展示名称的规则被覆盖为展示名称加可选备注摘要；文件名和路径仍不在首页展示
- (decision) 每张卡片备注为可选纯文字，支持新增、编辑和清空，不修改源文件
- (decision) 备注摘要选择 A：首页最多展示两行，超出截断并显示省略；管理界面可查看和编辑完整备注
- (decision) 备注长度选择 A：最多 500 个字符，换行计数，纯空白视为未填写；V1 仅纯文字，不含富文本、Markdown、预览或附件
- (decision) V1 范围选择 A：锁定当前全部核心能力加每卡片纯文字备注
- (decision) V1 明确排除收藏、最近打开、标签、备注搜索、批量添加、文件夹导入、导入导出、设备迁移和跨设备同步
- (event) 已将 Discovery 决策结构化为正式 PRD 草稿：4 条用户旅程、28 条唯一 FR、7 条 NFR、V1 范围、成功指标和风险约束
- (event) 已创建 addendum.md，承接桌面框架、存储格式、打包、路径规范化和升级机制等 Architecture 议题
- (gap) OQ-001 核心操作计数口径尚未确认，直接阻塞 5 秒和 2 次操作成功指标的最终验收定义
- (decision) 核心操作计数选择 A：类别选择或切换计一次，完整搜索词输入计一次，点击卡片计一次；鼠标移动、滚动和等待不计；选择打开方式异常流程排除
- (event) Discovery 完成，所有阶段阻塞问题已解决，进入 Finalize memlog 审计
- (event) Finalize memlog 审计：将 frontmatter topic 从旧 HTML 专用名称更新为已确认产品名本地文件导航工具；历史决策保留并由 override 覆盖
- (event) Finalize 输入对账通过：无阻塞遗漏或旧方案复活；发现知识上下文体验意图及技术方案取舍理由两项非阻塞差异，等待用户决定处理方式
- (decision) 输入对账差异选择 A：不将教练提出的恢复知识上下文写入通用文件工具愿景
- (event) 已在 addendum 补充 Tauri 与 Electron 的复杂度、熟悉度、安装体积和资源占用等历史取舍背景，不形成技术结论
- (decision) Reviewer Gate 选择 B：仅运行 PRD 七维质量量表，不增加边界异常或对抗审查
- (event) Reviewer Gate 七维量表完成：critical 0、high 0、medium 3、low 1；总体可进入 UX 与 Architecture，进入 Story/QA 前需闭合少数验收语义
- (decision) Reviewer M1 延期：源文件等价判定机制由 Architecture 负责，进入 Epic/Story 与 QA 前必须确定并使唯一性语义可测试
- (decision) Reviewer Low 指标弱项忽略：用户已主动取消长期使用指标，保留效率与打开可靠性作为 V1 成功指标
- (event) Reviewer M2 启动检查可观察边界与 M3 标题及名称边界进入逐项确认
- (change) 用户提出取消文件状态检查功能；该变更影响启动检查、打开前复核、文件不可用状态、自动恢复和修复流程，暂不修改 PRD，等待确认取消范围
- (override) 取消全部主动文件状态检查：不做启动批量检查或打开前预检查，点击卡片后直接请求操作系统处理
- (decision) 实际打开请求失败时必须明确提示并提供重新选择文件或删除卡片；不显示检查中、不预标记文件不可用、不维护自动恢复状态，失败后卡片数据保持不变
- (event) Reviewer M2 通过范围变更解决；PRD 已移除主动文件状态检查相关 FR，并将后续 FR 连续重编号为 FR-001 至 FR-026
- (decision) HTML 标题有效性选择 A：无法读取、缺少 title、为空或纯空白时回退无扩展名文件名；否则去除首尾空格后作为默认名称
- (decision) 名称长度选择 A：卡片名称最多 80 字符，类别名称最多 30 字符，均为单行纯文字且不允许换行，超限不得保存并明确提示
- (event) Reviewer M3 已解决；Reviewer Gate 三个 Medium 全部完成处置，准备重跑质量量表验证修订
- (event) Reviewer Gate 复审：critical 0、high 0、medium 1、low 1，结论可进入 UX 与 Architecture
- (change) 已机械修正 FR-017 与 FR-019 的可用卡片措辞，改为不预判状态并直接请求操作系统处理或定位
- (decision) 复审 Low 长期价值指标继续按用户既有决定忽略，不恢复长期使用指标
- (event) Finalize 开放项清理完成：Open Questions 0、Assumptions 0、NOTE FOR PM 0；结构审查提出合并非目标与 V1 范围，保留用户旅程和风险章节
- (decision) 结构润色选择 A：合并非目标与 V1 范围为单一章节，保留全部独特边界并同步后续章节编号
- (event) 文案润色完成建议审查：发现 6 处影响清晰度的最小修改，均不改变产品语义，等待用户确认应用
- (decision) 文案润色选择 A：应用全部 6 处最小清晰度修正，不改变产品语义
- (event) 结构与文案 polish 完成，准备最终 Reviewer Gate 复核和机械检查
- (event) 最终 Reviewer Gate 通过：critical 0、high 0、medium 1、low 1；唯一 Medium 为在文件管理器中显示失败反馈未定义
- (decision) 文件管理器定位失败选择 A：明确提示失败并提供重新选择文件和删除卡片；卡片名称、备注、类别和顺序保持不变
- (event) Reviewer Gate 最后一个 Medium 已解决，准备定点复核后关闭 PRD
- (event) 最终 Reviewer Gate 通过：critical 0、high 0、medium 0、low 0，无剩余发现
- (event) 外部 handoff 配置为空，本次仅交付本地 PRD 产物
- (event) PRD finalized
