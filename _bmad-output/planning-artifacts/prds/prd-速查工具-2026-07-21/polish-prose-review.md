| Original Text | Revised Text | Changes |
|---|---|---|
| 卡片展示名称是最多 80 个字符的单行纯文字；去除首尾空格后不能为空，且不得与现有卡片名称完全相同。 | 卡片展示名称须为单行纯文字，最多 80 个字符；去除首尾空格后不能为空，且不得与现有卡片名称完全相同。 | 调整语序，避免“是最多”的生硬表达。 |
| 类别名称是最多 30 个字符的单行纯文字，不允许换行；超过 30 个字符时不得保存，并提供明确长度提示。 | 类别名称须为单行纯文字，最多 30 个字符；超过限制时不得保存，并提供明确的长度提示。 | 调整语序，合并重复的换行限制，并明确“限制”所指。 |
| 产品不内嵌预览、渲染或编辑文件内容。 | 产品不在应用内预览、渲染或编辑文件内容。 | “在应用内”比“内嵌”更直接。 |
| 产品不在启动时或主打开动作前主动检查源文件状态；点击卡片后直接请求操作系统按默认方式处理源文件。 | 产品既不在启动时检查源文件状态，也不在执行主打开动作前检查；用户点击卡片后，产品直接请求操作系统按默认方式处理源文件。 | 用“既不……也不……”消除否定范围歧义。 |
| 用户可以新增、编辑或清空；操作不修改源文件。 | 用户可以新增、编辑或清空备注；这些操作不修改源文件。 | 补全宾语和指代，便于章节独立抽取。 |
| V1 不迁移、导入导出或持续同步两台设备的数据。 | V1 不提供两台设备之间的数据迁移、导入、导出或持续同步。 | 拆分并列动作，明确“两台设备之间”的修饰范围。 |
